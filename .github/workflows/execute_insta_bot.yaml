name: Run Insta-Bot

on:
  schedule:
    - cron: "* */6 * * *"
  workflow_dispatch:

defaults:
  run:
    shell: bash

jobs:
  insta:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Setup python
        uses: actions/setup-python@v4
        with: 
          python-version: "3.13"
      
      - name: Install Python dependencies
        run: |
          sudo apt-get update
          sudo apt-get install portaudio19-dev
          python3 -m pip install discord.py pyaudio

      - name: Execute insta-bot
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        run: python3 src/insta.py
      